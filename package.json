{
  "name": "pdf-proxy-api",
  "version": "1.0.0",
  "description": "PDF Proxy API for bypassing restrictions",
  "main": "api/pdf.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "node-fetch": "^2.6.7"
  },
  "engines": {
    "node": ">=14.x"
  }
}
